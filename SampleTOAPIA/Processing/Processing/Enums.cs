namespace Processing
{
    // Cursor Modes
    public enum MODE
    {
        ARROW,
        CROSS,
        HAND,
        MOVE,
        TEXT,
        WAIT
    }

    public enum FORMAT
    {
        RGB,
        ARGB,
        ALPHA
    }

    public enum BLENDMODE
    {
        BLEND,
        ADD,
        SUBTRACT,
        DARKETS,
        LIGHTEST,
        DIFFERENCE,
        EXCLUSION,
        MULTIPLY,
        SCREEN,
        OVERLAY,
        HARD_LIGHT,
        SOFT_LIGHT,
        DODGE,
        BURN
    }

    //public enum ColorMode
    //{
    //    RGB,
    //    HSB
    //}
}